#include <stdio.h>

/**
 * main - prints some words to stdout
 * Return: according to the Mighty Barbier, 0
*/
int main(void)
{
char *value = "\"Programming is like building a multilingual puzzle";
puts(value);
return (0);
}
