#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @n: number to be worked on
 * Return: absolute value, duhh
*/
int _abs(int n)
{
if (n < 0)
return (-1 * n);
return (n);
}
